# TODO List

- [ ] Review global `_conn_start_times` in `app/db.py` for multi-process safety.
- [ ] Ensure all API endpoints in `app/routes.py` are properly protected by authentication.
- [ ] Implement caching for Discord API responses in `app/auth.py` to mitigate rate limiting.
- [ ] Add input validation for all API endpoints to ensure received data is in the expected format.
